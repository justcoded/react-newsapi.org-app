import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    height: 45,
    width: '100%',
    backgroundColor: '#e7e7e7'
  },
  footerContent: {
    fontSize: 11,
    textAlign: 'center',
    padding: 15
  }
});
